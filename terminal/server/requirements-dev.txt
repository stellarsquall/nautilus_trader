# Frontend backend DEV/TEST-only dependencies (bolt-on).
#
# Not needed to run the app — only to run the backend test suite:
#
#   uv pip install -r terminal/server/requirements.txt
#   uv pip install -r terminal/server/requirements-dev.txt
#   cd terminal/server && pytest tests/
#
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # FastAPI TestClient (used by test_websocket.py / test_main.py)
