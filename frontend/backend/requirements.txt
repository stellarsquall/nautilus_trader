# Frontend backend Python dependencies (bolt-on).
#
# These are installed INTO the project's uv-managed `.venv` (which already
# contains a built `nautilus_trader`) by `frontend/run.sh`. They are kept out
# of the core `pyproject.toml` so the upstream project is never modified.
#
#   uv pip install -r frontend/backend/requirements.txt
#
fastapi>=0.110
uvicorn[standard]>=0.29
# Pulled in transitively by nautilus_trader's TestDataProvider.read_csv_ticks
# (fsspec GitHub backend imports `requests`); not installed by the base sync.
requests>=2.31
